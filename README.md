# portald11city

docker-compose exec web drush cr

docker-compose exec web drush <command>

docker compose run --rm sonar_scanner

root@424265f527ba:/opt/drupal# vendor/bin/phpunit --coverage-clover=coverage/clover.xml -c phpunit.xml.dist